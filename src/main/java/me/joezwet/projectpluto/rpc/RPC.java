package me.joezwet.projectpluto.rpc;

import me.joezwet.projectpluto.rpc.util.Strings;
import net.minecraftforge.fml.common.Mod;

@Mod(modid = Strings.MODID, clientSideOnly = true, name = Strings.NAME, version = Strings.VERSION, updateJSON = Strings.UPDATEJSON)
public class RPC {
}
